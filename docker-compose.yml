version: "3.2"

services:
  dev:
    container_name: anmol-node
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/anmol-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/anmol-node/.cargo
    volumes:
      - .:/var/www/anmol-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/anmol-node --dev --ws-external"
